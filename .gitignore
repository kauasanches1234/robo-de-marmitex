# dependências / build
node_modules/
dist/
*.log

# arquivos de sistema
.DS_Store
Thumbs.db

# config local do Claude Code (não versionar credenciais/estado)
.claude/settings.local.json

# symlink local usado pelos testes
tests/node_modules

# guias de operação e relatórios de segurança — NÃO versionar (privados do dono)
_local/
